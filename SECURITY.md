# Security & Privacy

> **TL;DR (English):** Your voice and transcribed text never leave your machine.
> The only network connection this app makes is a **one-time model download**
> from Hugging Face on first run. There is no telemetry, no analytics, no remote
> logging. The codebase is small and auditable. This document explains the
> OS-level capabilities the app uses and **why your antivirus may flag it
> (false positive)**.

ローカル音声入力ツールという性質上、本アプリは OS の低レベル機能（キー入力の監視・送出、クリップボード操作）を使います。これらは**情報窃取マルウェアが使う機能と同じ**ため、透明性を最優先に、何をしているか・していないかを明記します。

---

## 0. この文書が適用される範囲（先にお読みください）

**本文書は、次の URL で配布されているコードについてのみ有効です。**

> **https://github.com/TTMK7777/voice-typing**

本リポジトリは MIT ライセンスであり、**誰でも fork して改変・再配布できます。そして改変版に本文書がそのまま同梱されていても、本文書は改変版について何も保証しません。**

このことは、本文書の性質上とくに重要です。本文書は §3 で「アンチウイルスの警告は誤検知です」と説明していますが、**その説明は、悪意あるコードを混ぜた改変版にとっては「被害者にアンチウイルスを無視させるための材料」として機能してしまいます。**

したがって:

- **入手元が上記 URL と異なる場合、この文書を信用しないでください。**
- 本文書の「誤検知です」という記述は、**あなた自身が中身を確認したコードに対してのみ**適用してください。確認していないコードについては、**アンチウイルスの警告に従ってください。**
- 確認は現実的な作業量です。ネットワーク送信の有無は `core.py`（約 250 行）を読めば分かります。`import` に `requests` / `httpx` / `urllib` / `socket` が無いこと、そうしたライブラリの呼び出しが無いことを確認してください。

**「透明性を主張する文書ほど、悪用されたときに効果が大きい」** — この非対称性を理解した上で、本文書をお使いください。

---

## 1. データはどこにも出ません

| 処理 | 実行場所 | 外部送信 |
|---|---|---|
| 録音 | あなたの PC（マイク → メモリ上の音声バッファ） | なし |
| ウェイクワード待受（ON のときのみ） | あなたの PC（マイク → メモリ上の音声バッファ → ローカルの Whisper で判定） | なし |
| 文字起こし | あなたの GPU（faster-whisper をローカル実行） | なし |
| 貼り付け | あなたの PC（クリップボード → アクティブウィンドウ） | なし |

- **テレメトリ・解析・リモートログは一切ありません。**
- コードはネットワークへ音声・テキストを送る処理を持ちません（`core.py`）。

### 唯一の外部接続：初回のモデルダウンロード
faster-whisper は初回起動時に Whisper モデル（`large-v3` / `small`）を **Hugging Face からダウンロード**します。これは**モデルの取得のみ**で、あなたの音声・テキストがアップロードされることはありません。ダウンロード後はネット接続不要で動作します。

---

## 2. 使う OS 権限と、その理由

| 使う機能 | 実装 | なぜ必要か | していないこと |
|---|---|---|---|
| **グローバルホットキー監視** | `pynput.GlobalHotKeys`（`app_rt.py` / `app.py`） | どのアプリ使用中でも `Ctrl+Alt+Space` で録音を開始/停止するため | **キー入力の記録・保存・送信は一切しません。** 登録したホットキーの一致判定にのみ使い、他のキーには何もしません |
| **低レベルキーボードフック** | `SetWindowsHookExW`（`app_rt.py` の `_on_low_level_key()`） | **録音中に限り** Enter で録音を停止し、その Enter が前面アプリへ漏れて誤送信・誤改行するのを遮断するため | 監視するのは Enter キーのみで、他のキーは判定せずそのまま OS に流します（記録・保存・送信なし）。録音していないときは何も遮断しません |
| **キーストローク送出** | `pynput.keyboard.Controller`（`core.py` の `paste()` / `run_voice_command()`） | 確定テキストを「今カーソルがある場所」に貼るため `Ctrl+V` を送出。加えて音声コマンド（`/clear` 等）を実行するときのみ `Enter` を送出 | 送出するのは `Ctrl+V` と `Enter` だけです。任意のテキストをキー入力として1文字ずつ打つことはしません |
| **クリップボード操作** | `pyperclip`（`core.py` の `paste()` / `_restore_clipboard()`） | 貼り付けの実体（OS 横断で確実なため）。**復元のために、貼り付け前にあなたの現在のクリップボード内容を一度読み取り、貼り付け後に書き戻します** | 読み取った内容は**復元にのみ使い、プロセスのメモリ上にのみ保持**します。ファイル保存・履歴化・送信は一切しません。復元処理が終われば参照は破棄されます |
| **マイク録音** | `sounddevice`（`core.py` の `start_recording()`） | 音声入力のため | 録音データはメモリ上のみ。ファイル保存も送信もしません |
| **マイクの常時監視** | `sounddevice`（`core.py` の `start_monitor()`） | **ウェイクワード待受を ON にしている間だけ**、「やっほークロード」と言われたかを判定するため | **既定では OFF** で、ON にしない限りマイクは開きません。ON の間もローカルの Whisper で判定するだけで、音声はメモリ上のリングバッファのみ。判定が終われば破棄し、ファイル保存も送信もしません。録音中は待受側のマイクを閉じます |

**要点：** "global keyboard listener" は技術上すべてのキーイベントを受け取りますが、本アプリのコードは**登録ホットキーに一致したときだけコールバックを発火**し、それ以外のキーは記録も保存も送信もしません（`core.py` / `app_rt.py` を読めば確認できます）。

---

## 3. アンチウイルス／EDR が誤検知する可能性について

本アプリの「**グローバルなキー入力監視 ＋ キーストローク送出 ＋ クリップボード操作**」という組み合わせは、キーロガーや情報窃取マルウェアの典型的な挙動と**シグネチャ上は区別がつきません**。そのため一部の AV/EDR がヒューリスティックで警告を出すことがあります。

これは**誤検知（false positive）**です。理由：

1. **送信先がない** — 外部にデータを送るコードが存在しません（§1 参照）。
2. **記録しない** — キーイベントを保存・蓄積するコードが存在しません（§2 参照）。
3. **監査可能** — アプリ本体は約 1,200 行（空行含む、`core.py` 283 / `app_rt.py` 477 / `app.py` 200 / `wake_listener.py` 157 / `wake_word.py` 53 / `cuda_setup.py` 31）・依存も明示（`requirements.txt`）。

不安な場合は、ご自身またはセキュリティ担当の方が **`core.py`** を直接確認してください。**マイク録音・クリップボード操作・キーストローク送出という「外部に触れる」処理はすべてこの1ファイルに集約されており**、GUI 側（`app_rt.py` / `app.py`）はどちらも `core.py` の `VoiceCore` を呼ぶだけです（`from core import VoiceCore`）。GUI 側が独自に録音・貼り付けを行っている箇所はありません。

ウェイクワード待受で追加された `wake_listener.py` / `wake_word.py` も**マイクやネットワークには一切触れません**。マイクを開くのは `core.py` の `start_monitor()` だけで、この 2 ファイルは渡された音声ブロックと文字列を判定するだけです（そのため GPU の無い環境でも `tests/test_wake_word.py` で単体テストできます）。

唯一 `core.py` の外にある OS 低レベル処理は、`app_rt.py` の**録音中の Enter 遮断フック**（上表参照）だけです。

---

## 4. 既知の限界（設計上のトレードオフ）

- **クリップボード経由の貼り付け：** 確定テキストは一瞬グローバルクリップボードに乗ります（貼り付け直後に元内容を復元）。この短い窓の間、クリップボード監視系アプリが内容を拾える可能性は理論上あります。OS 横断で確実に貼るための設計上の割り切りです。
- **クリップボード復元のタイミング（残存リスク・未解決）：** `Ctrl+V` 送出から `PASTE_SETTLE_SEC`（既定 0.4 秒、`core.py`）待ってから元の内容へ戻します。**貼り付け先アプリの処理がこれより遅い場合、復元後の「元の内容」が貼られる可能性があります。** 直前にパスワード等をコピーしていた場合、それが貼り付け先に出てしまう、という失敗の仕方をします。
  - 緩和として、復元直前にクリップボードが自分の書いた内容のままかを確認し、別プロセスが書き換えていれば復元しません（他アプリの内容を踏み潰さないため）。
  - **ただしこれは競合そのものの解決ではありません。** Windows には「クリップボードが読み取られた」ことを知る一般的な手段が無く、待ち時間による確率的な緩和にとどまります。完全な解決には遅延レンダリング（`WM_RENDERFORMAT`）を使った実装が必要で、現時点では未実装です。
  - **機密情報をクリップボードに置いた直後の口述は避けてください。** 気になる場合は `VoiceCore(restore_clipboard=False)` で復元自体を無効化できます（この場合、クリップボードには認識テキストが残ります）。
- **貼り付け先：** 「今アクティブなウィンドウ」に貼ります。録音→確定の間にフォーカスを別の場所へ移すと、そちらに入力されます。
- **ウェイクワードの誤発火（待受 ON のときのみ）：** 「やっほークロード」に近い音（TV・動画・同居人の会話など）を拾うと、意図しない録音が始まり、その後に拾った音声が**アクティブなウィンドウに貼り付けられます**。「やっほー」は日常会話でまず出ない語を選んで確率を下げていますが、ゼロにはできません。
  - 待受は**既定で OFF**です。ON にするのは、貼り付いても困らない画面に向かっているときにしてください。
  - 何を拾って何と判定したかは標準出力に `[wake] '...' -> HIT/ignore` として全部出ます。誤発火が多い場合は `wake_listener.py` の `SPEECH_RMS`（拾い始める音量）を上げてください。
  - 待受から始まった録音は、無音 `AUTO_STOP_SILENCE_SEC`（既定 1 秒）で自動確定します。空振りしても `WAKE_START_GRACE_SEC`（既定 5 秒）で終了します。
- **音声コマンドの誤発火：** 「クロード、クリア」等の発話を検知すると `/clear` を貼って Enter を送ります。トリガー語必須・後続語ありは実行しないという誤発火対策は入れていますが、認識結果に依存するため 100% ではありません。不要なら `core.py` の `VOICE_COMMANDS` を空にすれば無効化できます。

---

## 5. 脆弱性の報告

**公開 Issue には書かないでください。** 内容に応じて次の窓口を使ってください。

| 状況 | 窓口 |
|---|---|
| 一般的な不具合・懸念（機微でないもの） | このリポジトリの Issue |
| 脆弱性など機微な内容 | [private security advisory](../../security/advisories/new)（メンテナのみ閲覧可） |
| メンテナ自身の行為が問題である場合 | [GitHub への通報](https://github.com/contact/report-abuse) |

個人の連絡先メールアドレスは公開していません。可能な範囲で速やかに対応します。

---

## 6. ライセンス

[MIT License](LICENSE)。無保証（"AS IS"）です。各自の責任でご利用ください。
