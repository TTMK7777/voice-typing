# voice-typing — STATUS

> このリポジトリの「現在地」の正本。`todo.md` は `.gitignore` 対象（ローカル限定）のため、共有される現在地はこちらに置く。

## 現在地（2026-09-12 棚卸し）
- 状態: 継続（公開済み・機能追加が続いている個人ツール）
- 現在地: ローカル完結の日本語音声入力ツールとして公開版の整備は完了。直近は起動時間の短縮（モデルロード中から録音を開始できる）を入れたところ。
- 最終作業: 2026-09-02 feat: 起動直後(モデルロード中)から録音を始められるようにする (#6)
- 次の一手: 任意 3 件——(1) ログのファイル出力（`pythonw` 起動だとログが 1 行も見えず不具合の切り分けができない。`safe_log()` 1 箇所の変更で済む）(2) CPU 対応（現状 `device="cuda"` 固定で NVIDIA GPU 必須。CONTRIBUTING.md でも歓迎テーマとして明記済み）(3) CI 導入（GPU 前提でグリーンにできる保証がないため保留。入れるなら GPU 不要の `tests/test_core.py` に絞る）
- 判断待ち: (1) PR #6 の「起動→即喋る」体感確認が未実施（実機が必要）(2) ログファイル出力実装の要否判断 (3) CHANGELOG.md 新設の要否（変更履歴が README とローカルメモに分散気味）

## open な Issue / PR
- Issues 0 件 / PR 0 件（2026-09-12 時点）
