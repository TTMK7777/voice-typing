# Contributing

Issue / Pull Request を歓迎します。個人プロジェクトのため反応が遅れることはありますが、いただいたものは必ず読みます。

## このプロジェクトの方針

- **完全ローカルであることが最優先。** 音声・テキストを外部へ送る変更（クラウド API での文字起こし、テレメトリ、クラッシュレポート送信など）は、たとえオプトインでも受け入れません。唯一の例外は既存の初回モデルダウンロード（Hugging Face）です。
- **日本語の認識品質**（句読点・同音異義語・幻覚除去）に価値を集中させています。
- **軽さを保つ。** 依存を1つ増やすことは、それだけでこのツールの魅力を削ります。追加する場合は理由を書いてください。
- 機能は足すより削る方向で考えます。過去に外した機能とその理由は [README のロードマップ](README.md#ロードマップ) にあります。

## 開発環境

```powershell
cd path\to\voice-typing
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

動作前提は **Windows + NVIDIA GPU (CUDA)** です（`device="cuda"` 固定）。CPU 対応は歓迎する改善テーマです。

## テスト

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

- テストは標準ライブラリの `unittest` のみで書きます（追加のテスト依存は入れない方針）。
- マイク・GPU・Whisper モデルに依存させないでください。録音とモデル呼び出しは差し替えて書きます。
- 挙動を変える PR には、その挙動を確認するテストを添えてください。

## PR の出し方

1. 目的を1〜2行で説明してください（何が困っていて、どう直すか）。
2. 1 PR = 1 テーマにしてください。
3. `python -m unittest discover -s tests` が通ることを確認してください。
4. GUI・貼り付け・ホットキー周りを変えた場合は、**実機で試した結果**を書いてください（どのアプリで、何をして、どうなったか）。この領域は自動テストで担保しきれません。

## コードのスタイル

- 既存ファイルの書き方に合わせてください（日本語コメント、`"""docstring"""`、標準ライブラリ優先）。
- コメントは「なぜそうしたか」を書いてください。「何をしているか」はコードが語る部分です。

## 報告してもらえると助かること

- 認識精度の実例（どう話して、どう出たか）。日本語の口述は環境差が大きく、実例が一番効きます。
- アンチウイルスによる誤検知の報告（製品名と検出名）。[SECURITY.md](SECURITY.md) の説明を改善できます。

## 行動規範・脆弱性報告

- 行動規範: [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)
- 脆弱性の報告: [SECURITY.md](SECURITY.md)（公開 Issue には書かないでください）
